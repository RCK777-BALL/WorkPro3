{
  "name": "workpro3",
  "version": "1.0.0",
  "description": "This repository contains a small full‑stack app split into two folders:",
  "private": true,
  "main": "fixWorkPro.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "jest",
    "postinstall": "npm ci --silent --prefix backend && npm ci --silent --prefix frontend",
    "fix:backend": "node fixWorkPro.js backend",
    "fix:frontend": "node fixWorkPro.js frontend",
    "fix:all": "node fixWorkPro.js all",
    "lint": "npm --prefix backend run lint && npm --prefix frontend run lint",
    "lint:biome": "biome check backend frontend",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck",
    "test:unit": "npm --prefix backend run test && npm --prefix frontend run test",
    "audit:backend": "npm audit --prefix backend --audit-level=low",
    "audit:frontend": "npm audit --prefix frontend --audit-level=low",
    "audit:prod": "npm audit --prefix backend --omit=dev --audit-level=low && npm audit --prefix frontend --omit=dev --audit-level=low",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "build:frontend": "npm --prefix frontend run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/RCK777-BALL/WORKPRO3.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "bugs": {
    "url": "https://github.com/RCK777-BALL/WORKPRO3/issues"
  },
  "homepage": "https://github.com/RCK777-BALL/WORKPRO3#readme",
  "dependencies": {
    "fs-extra": "^11.3.2",
    "glob": "^13.0.3",
    "mongoose": "^9.2.1",
    "ts-morph": "^27.0.2"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.2",
    "@types/jest": "^30.0.0",
    "@types/node": "^25.2.3",
    "jest": "^30.2.0",
    "mongodb-memory-server": "^10.2.3",
    "supertest": "^7.2.2",
    "ts-jest": "^29.4.5",
    "typescript": "^5.9.3"
  }
}
